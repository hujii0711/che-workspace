schemaVersion: 2.2.0
metadata:
  name: my-workspace1111
  
commands:
  - id: install-extension
    exec:
      component: tools
      commandLine: >
        curl -L -o /tmp/websquare-web-extension.vsix https://hujii0711.github.io/savezone/websquare-web-extension-1.0.0.vsix
      workingDir: /projects

events:
  postStart:
    - install-extension
