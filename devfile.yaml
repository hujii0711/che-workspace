schemaVersion: 2.2.0
metadata:
  #name: vscode-websquare-0.0.19
  generateName: vscode-websquare # 고정 이름 대신 동적 생성
components:
  - name: tools
    container:
      image: quay.io/devfile/universal-developer-image:latest
      memoryLimit: 2Gi
      mountSources: true
      env:
        - name: DEFAULT_EXTENSIONS
          value: "/tmp/websquare-web-https-extension-2.0.0.vsix;" #/tmp/vmware.vscode-boot-dev-pack-0.2.2.vsix;/tmp/ms-ceintl.vscode-language-pack-ko-1.105.2025091709.vsix
      volumeMounts:
        - name: tmp
          path: /tmp
  - name: tmp
    volume:
      size: 1Gi
  # - name: secret-certs
  #   volume: {}
  - name: vscode-backend-server
    container:
      image: localhost:5000/vscode-backend-https-image:0.0.1
      command: ["/bin/sh", "-c"]
      args: ["node dist/app.js"]
      memoryLimit: 4Gi
      # /projects 디렉터리 자동 마운트
      mountSources: true
      #시크릿 타입이 tls이므로 쿠버네티스가 자동으로 다음 이름으로 파일을 생성 /certs/tls.key, /certs/tls.crt
      # volumeMounts:
      #   - name: secret-certs
      #     path: /webserver/certs
      endpoints:
        - name: vscode-backend
          targetPort: 11199
          # 외부 접근 허용 (필요 시 internal 유지)
          exposure: public
          # HTTPS 프로토콜 명시
          protocol: https
          # TLS 활성화
          secure: true
      env:
      - name: NODE_ENV
        value: "production"
    attributes:
      # DevWorkspace가 Secret을 자동으로 컨테이너에 마운트하도록 지시
      controller.devfile.io/volume-mount-secret:
        secret-certs:
          # 실제 쿠버네티스 Secret 리소스 이름
          secretName: vscode-certs
commands:
  - id: install-websquare-vsix
    exec:
      component: tools
      commandLine: |
        curl -L --fail --silent --show-error -o /tmp/websquare-web-https-extension-2.0.0.vsix https://hujii0711.github.io/savezone/websquare-web-https-extension-2.0.0.vsix || { echo "Failed to download websquare-web-https-extension-2.0.0.vsix"; exit 1; }
      group:
        kind: run
  #- id: install-springboot-vsix
  #  exec:
  #    component: tools
  #    commandLine: |
  #      curl -L --fail --silent --show-error -o /tmp/vmware.vscode-boot-dev-pack-0.2.2.vsix https://hujii0711.github.io/savezone/vmware.vscode-boot-dev-pack-0.2.2.vsix || { echo "Failed to download vmware.vscode-boot-dev-pack-0.2.2.vsix"; exit 1; }
  #    group:
  #      kind: run
  #- id: install-ko-langpack-vsix
  #  exec:
  #    component: tools
  #    commandLine: |
  #      curl -L --fail --silent --show-error -o /tmp/ms-ceintl.vscode-language-pack-ko-1.105.2025091709.vsix https://hujii0711.github.io/savezone/ms-ceintl.vscode-language-pack-ko-1.105.2025091709.vsix || { echo "Failed to download vscode-language-pack-ko-1.105.2025091709.vsix"; exit 1; }
  #    group:
  #      kind: run
events:
  postStart:
    - install-websquare-vsix
    #- install-springboot-vsix
    #- install-ko-langpack-vsix
